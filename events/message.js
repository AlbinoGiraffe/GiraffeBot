module.exports = {
	name: 'messageCreate',
	execute(message) {
		// if (message.author == client) return;
		// console.log(
		// 	`${message.author.tag} in #${message.channel.name}: ${message.content}`,
		// );
		// if (message.startswith(`${prefix}ud`)) {
		// 	message.channel.send(urban(message.content.replace(`${prefix}ud`)));
		// }
	},
};
